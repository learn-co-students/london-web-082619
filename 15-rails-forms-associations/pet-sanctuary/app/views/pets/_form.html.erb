<%= form_for(@pet) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :age %>
  <%= f.text_field :age %>

  <%= f.label :user %>
  <%= f.collection_select(:user_id, User.all, :id, :username) %>

  <%= f.submit %>
<% end %>
